#!/usr/bin/python
import mce_data
import matplotlib.pyplot as plt 
f = mce_data.SmallMCEFile('/data/cryo/testfile2')
d = f.Read(row_col=True, unfilter='DC').data
print d.shape

i = 0
T = range(d.shape[0])
for i in range(d.shape[1]):
	plt.plot(T,d[:,i])

plt.show()

